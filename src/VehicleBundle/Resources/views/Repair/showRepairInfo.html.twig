{% extends 'base.html.twig' %}

{% block title %}{{ 'Repair'|trans }}{% endblock %}
{% block body %}

    <table class="table">
        <tbody>
{% for repair in repair %}
            <tr>
                <th>{{ 'Description'|trans }}</th>
                <td>{{ repair.description }}</td>
            </tr>
            <tr>
                <th>{{ 'Date'|trans }}</th>
                <td>{% if repair.date %}{{ repair.date|date('Y-m-d') }}{% endif %}</td>
            </tr>
            <tr>
                <th>{{ 'Amount'|trans }}</th>
                <td>{{ repair.amount }}</td>
            </tr>
            {% endfor %}
        </tbody>
    </table>
{% endblock %}
