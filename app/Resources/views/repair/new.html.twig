{% extends 'base.html.twig' %}

{% block title %}{{ 'Repair creation'|trans }}{% endblock title %}
{% block body %}

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="{{ 'Create'|trans }}" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('show_vehicle_repo', {'id': id}) }}">{{ 'Back'|trans }}</a>
        </li>
    </ul>
{% endblock %}
