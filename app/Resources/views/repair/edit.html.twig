{% extends 'base.html.twig' %}

{% block desc %}<a href="{{ path('repair_show', { 'id': repair.id }) }}" class="badge">{{ 'Back'|trans }}</a>{% endblock desc %}
{% block title  %}{{ 'Repair edit'|trans }}{% endblock %}
{% block body %}

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="{{ 'Edit'|trans }}" class="btn btn-primary" />
    {{ form_end(edit_form) }}

{% endblock %}
