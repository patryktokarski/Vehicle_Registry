{% extends 'base.html.twig' %}

{% block desc %}<a href="{{ path('show_vehicle_repo', { 'id': refuel.car.id }) }}" class="badge">{{ 'Back'|trans }}</a>{% endblock desc %}
{% block title %}Refuel edit{% endblock title %}
{% block body %}

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="{{ 'Edit'|trans }}" class="btn btn-success" />
    {{ form_end(edit_form) }}
<hr>
    {{ form_start(delete_form) }}
        <input type="submit" id="delete" value="{{ 'Delete'|trans }}" class="btn btn-danger">
    {{ form_end(delete_form) }}

{% endblock %}
{% block js %}<script src="{{ asset('sbadmin/js/confirm_delete.js') }}"></script>{%endblock js %}
