{% extends 'base.html.twig' %}

{% block title %}Model creation{% endblock title %}
{% block desc %}<a href="{{ path('model_index') }}" class="badge">Back</a>{% endblock desc %}

{% block body %}

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" class="btn btn-success"/>
    {{ form_end(form) }}

{% endblock %}
