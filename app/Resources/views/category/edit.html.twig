{% extends 'base.html.twig' %}

{% block title %}{{ 'Category edit'|trans }}{% endblock title %}
{% block desc %}<a href="{{ path('category_index') }}" class="badge">Back</a>{% endblock desc %}
{% block body %}

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="Edit" class="btn btn-success" />
    {{ form_end(edit_form) }}

{% endblock %}
